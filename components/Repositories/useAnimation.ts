import { useEffect } from "react"
import gsap from 'gsap'

function useAnimation() {
    useEffect(() => {
        gsap.to('#repositories', {
            
        })
    }, [])
}

export default useAnimation